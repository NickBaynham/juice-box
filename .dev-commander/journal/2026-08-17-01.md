# 2026-08-17 entry 01

W0 foundation is complete: pdm project, settings, FastAPI app factory with GET /health, uvicorn entrypoint, container image, Compose with PostgreSQL 17, and a green CI workflow. All eight increments of plan 0001 are committed and pushed. Increments 4-8 were implemented by subagents, one per increment, each with an independent verification-based review; increments 1-3 were implemented directly. Four errors were found in the plan text and corrected in commit 37b43b0, with a Corrections section appended rather than a silent rewrite: the Dockerfile instruction order was not executable because increment 1 sets distribution = true, the CI template path did not exist, the TODO range omitted W12, and increment 8 shipped a different commit subject across two commits. Two defects were repaired that the plan did not anticipate: CI checked out shallow while gitleaks scans BASE^..HEAD, so every multi-commit push failed and the secret scan had never covered more than one commit; and Settings refused to start on any unrecognised .env key while the docs promote .env as the configuration mechanism. Unit tests were also isolated from ambient JUICEBOX_ variables, which would have gone red in W1 the moment developers set JUICEBOX_DATABASE_URL. Review 0003 records eight minors carried into W1 and three documents needing amendment: ADR-0001 describes a CI service container that does not exist, design 0002 criterion 2 requires an acceptance directory that does not exist until W12, and criterion 7 penalises self-review corrections found after committing.
